	流程控制语句			【重点】
		顺序结构
		选择（分支）结构
			if
			switch
		循环结构
	Random类				【重点】
	
	
选择（分支）结构
	if语句
	
	switch语句
		格式
			switch(表达式) {
				case 值1:
					语句体1;
					break;
				case 值2:
					语句体2;
					break;
				...
				default:
					语句体n;
					break;
			}
			
		执行流程
			
			
		案例
			
		注意事项
			1、case后面的值只能是常量不能是变量
				并且case后面的常量不能有重复的值
			2、switch语句小括号中的表达式只能是以下几种类型：
				byte short int char -- 基本类型中可以自动提升为int类型的都可以
				String				-- JDK7.0
				枚举				-- JDK5.0
			3、default语句可以写出来，也可以省略，但是一般建议加上，可以对都不满足条件的情况进行统一处理
			   如果写出来，default的位置可以任意的，可以在所有的case语句之下，也可以在所有的case语句之上
			   ，甚至可以在多个case语句之间，default的位置不同不会影响程序的执行流程（前提：语句中不能省略掉break），但是
			   default写出来一般都是在所有的case语句之后
			
			4、switch语句中的break可以省略，省略之后可能会出现case穿透问题。
				switch语句结束的条件有两个：1是碰到break，2是碰到switch的右大括号
				

循环结构
	for循环
		格式
			for(初始化语句;条件控制语句;循环控制语句) {
				循环体语句;
			}
			
			初始化语句只会执行一次
			条件控制语句是绝对是否继续执行的条件，如果它的结果为false，有可能循环体语句一次都不执行
			
		执行流程
		案例
			求和思想
			
					// 1. 需求是要求和, 定义求和变量.
					int sum = 0;
					// 2. 循环中获取1-100之间的每一个数值
					for(int i = 2; i <= 100; i+=2){
						
						sum += i;
						
					}
					
					// 5. 循环结束后, 打印最终结果
					System.out.println("1-100之间的偶数和为:" + sum);
			
			计数器思想
				
		
			
		注意事项
	
	while循环
		格式：
			while(条件控制语句){
				循环体语句;
			}
			
			初始化语句;
			while(条件控制语句){
				循环体语句;
				循环控制语句;
			}
			
		
	
	do...while循环
		格式：	
			do{
				循环体语句;
			}while(条件控制语句);
			
			
			初始化语句;
			do{
				循环体语句;
				循环控制语句;
			}while(条件控制语句);
			
		特点：
			至少会执行一次循环体
			先执行一次循环体，再判断条件，如果条件满足会继续执行循环体，如果条件不满足则不会再执行了
			
			
	死循环
		for(;;){
		}
		
		while(true){
		}
		
	循环跳转语句
		continue
			使用场景：在循环中
			执行特点：结束当次循环，继续下一次循环
		
		break
			使用场景：在循环和switch语句中
			执行特点：结束整个循环
			
		
		面试题：continue和break的区别
		
Random : 产生随机数
	1. 导包	: import java.util.Random;
				导包的动作必须出现在类定义的上面

	2. 创建对象 : Random r = new Random();
				上面这个格式里面，r 是变量名，可以变，其他的都不允许变

	3. 获取随机数 : int number = r.nextInt(10);	//获取数据的范围：[0,10) 包括0,不包括10
				上面这个格式里面，number是变量名，可以变，数字10可以变。其他的都不允许变	
